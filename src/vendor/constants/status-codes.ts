export enum StatusCodes {
  // Success
  Success = 200,
  Created = 201,
  // Client Error
  BadRequest = 400,
  Unauthorized = 401,
  // Forbidden = 403,
  NotFound = 404,
  // Server Error
  InternalServerError = 500,
}
